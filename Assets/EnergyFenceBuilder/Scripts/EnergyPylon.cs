using UnityEngine;

namespace EnergyFenceTool
{
    public class EnergyPylon : MonoBehaviour
    {
        
        // This class is only used to find pylon objects when building fence

        // Start is called before the first frame update
        void Start()
        {   
        }

        void Awake()
        {   
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}